namespace Tsubasa
{
    public class Constants
    {
        //load the config file as a json
        // static JObject _jObject = new JSONManager().readJSON("./config.json");

        //get token and prefix from the config
        //public static string token = _jObject.GetValue("token").ToString();
        //public static string prefix = _jObject.GetValue("prefix").ToString();
    }
}